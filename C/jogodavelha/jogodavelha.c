#include <stdio.h>

struct player
{
    char nome[50];
    int win;
    int lose;
};
int main()
{
    printf("Escolha uma opção: ");
    scanf("%d", &op);
    switch(op)
    {
        case '1':
    }
}